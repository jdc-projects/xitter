# Local edge - mirrors the cluster's Traefik path-based routing.
# Dynamic config lives in dynamic/ and is templated with XITTER_* env vars.
providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

entryPoints:
  web:
    address: ':80'

api:
  dashboard: true

log:
  level: INFO
