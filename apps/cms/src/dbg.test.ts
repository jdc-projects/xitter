// Temporary scratch file from local debugging - not committed.
import { describe, expect, it } from 'vitest';

describe('scratch', () => {
  it('passes', () => {
    expect(true).toBe(true);
  });
});
